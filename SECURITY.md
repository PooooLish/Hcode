# Security Policy

## 当前安全状态

Hcode 是开发预览版，能够读取和修改文件、执行命令并访问外部模型服务。它不应被视为强隔离的生产执行环境。

已知边界：

- macOS 可使用 Seatbelt，Linux 可使用 Bubblewrap；Windows 当前没有操作系统级沙箱后端。
- 权限检查降低误操作概率，但不能替代容器、虚拟机或受限系统账号。
- Remote、MCP、Hooks、Skills 和子 Agent 会扩大工具与网络边界，启用前应单独审查。
- `bypassPermissions` 会跳过常规人工批准，只应在一次性、隔离且可恢复的环境中使用。

## 安全使用建议

- 默认使用 `default` 权限模式。
- 在干净、受 Git 管理且有远程备份的测试仓库中运行。
- 对不受信任的任务使用容器或虚拟机，并默认关闭网络。
- 密钥只通过进程环境变量注入，不写入 YAML、日志、提示词、测试夹具或 issue。
- 使用专用测试账号、最小权限和可撤销密钥。
- 执行前检查模型生成的命令和文件修改范围。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告渠道。报告应包含受影响版本、复现步骤、影响范围和可安全共享的最小样例。不要在公开 issue 中提交密钥、私有仓库内容或可直接利用的敏感细节。

## 修复优先级

1. 补齐 Windows 隔离执行后端。
2. 为文件搜索、Plan 模式、Remote 和权限切换增加越界回归测试。
3. 对工具执行、权限决策和网络访问生成可审计事件。
4. 在发布稳定版本前完成独立安全复查。
